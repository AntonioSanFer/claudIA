"""ClaudIA — a provider bridge for Claude Code.

ClaudIA launches the user's installed `claude` binary against a local LiteLLM
proxy, letting Claude Code talk to any OpenAI-compatible provider. See AGENTS.md
for the full specification.
"""

__version__ = "0.3.0"
